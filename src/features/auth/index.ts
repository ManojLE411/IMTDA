// Context & Hooks
export { AuthProvider, useAuth } from './context/AuthContext';

// Components
export { AuthPage } from './components/AuthPage';

// Types
export type { Student } from './types/auth.types';

// API (if needed externally)
export { authStorage } from './api/authStorage';

